# 전자기력 실험실 — electromagnetic-force

## ① 앱의 목적과 대상 단원

- **목적** : 중학교 2학년 과학 「Ⅱ. 전기와 자기」 **2. 자기**의 **전자기력**을
  실험실 · 미션 · 학습지로 익히게 한다.
- **대상** : 중학교 2학년 · 과학 (2022 개정)
- **성취기준 (원문 그대로 · 확인함)**

  > **[9과14-04]** 자기장 안에 놓인 전류가 흐르는 코일이 받는 힘의 특성을 추리하고,
  > 전동기 등 일상생활에서 활용한 예를 찾을 수 있다.
  > `<탐구 활동>` 전류가 흐르는 코일 주위의 자기장 관찰하기

  성취수준 A~E 의 표현도 학습지에 반영했다 — E 「힘을 받게 됨을 말할 수 있다」 →
  D 「이를 활용한 예를 찾는다」 → C 「전류 방향과 자기장 방향으로 힘의 방향을 찾는다」 →
  B/A 「원리를 설명·적용한다」.

### 출처

```
C:\김미란(2023.11.20)\학습자료(2023.11.20)\(중)2022 개정 교육과정에 따른 성취수준(과학) (1).hwp
  → [9과14-04] 원문 · 탐구 활동 · 성취수준 A~E
C:\김미란(2023.11.20)\학습자료(2023.11.20)\2024.2학년\2. 전기와 자기.hwp
  → Ⅱ. 전기와 자기 / 2. 자기 : (1) 전류가 만드는 자기장 (65~67쪽) · (2) 전기와 자기가 만드는 힘 (68~71쪽) · 전동기
```

| 선생님 학습지의 내용 | 이 앱에서 |
|---|---|
| 자기장의 방향 = 나침반 N극이 가리키는 방향 · 오른손 규칙 | 🧭 직선 도선 · 학습지 1~4번 |
| 코일 주위 자기장 · 전자석 | 🧭 코일 · 학습지 5~6번 |
| 「해보기」 알루미늄박 실험표 (기본 → **위쪽** / 극 바꿈 → 아래쪽 / 전류 바꿈 → 아래쪽 / 둘 다 → 위쪽) | ⚡ · 미션 4~6 · 학습지 7번 표 |
| 힘의 방향은 전류·자기장의 방향, 크기는 **비례** | ⚡ · 미션 7 · 학습지 8~10번 |
| 형성평가 「더 많이 움직이려면 저항을 **작게**」 | ⚡ 저항 슬라이더 · 학습지 11번 |
| 전동기 : 자석 + 코일, 두 변의 전류가 반대 → 힘이 반대 → 회전 | 🌀 · 미션 8 · 학습지 14~18번 |
| 정류자 : 반 바퀴 뒤 전류를 끊어 한 방향으로 계속 회전(관성) | 🌀 (전류가 끊기는 반 바퀴를 회색으로) · 학습지 19번 |

> ⚠ **「연습하기」 그림(A · C)과 형성평가의 회로 그림은 hwp 안의 그림이라 읽지 못했다.**
> 같은 유형의 문항을 새로 만들었다. 선생님이 원본과 견주어 주면 좋다.
> ⚠ 3학년 `electromagnetic-induction`(전자기 유도)과 **거꾸로 가는 현상**이다 —
> 전동기 : 전기 → 운동 / 발전기 : 운동 → 전기. 화면 아래 표에서 이어 준다.

### 왜 이렇게 만들었나 (설계 의도)

**⊙ / ⊗ 기호 하나가 세 장면을 잇는다.** 전류가 (화면에서) 나오는 방향이 ⊙, 들어가는 방향이 ⊗ 다.

1. **🧭** ⊙ 이면 나침반이 **반시계**로 도선을 둘러싼다. (⊗ 이면 시계)
2. **⚡** 같은 ⊙ 를 자석 사이에 놓으면 **위쪽**으로 힘을 받는다. (⊗ 이면 아래쪽)
3. **🌀** 코일의 한 변은 ⊙ , 반대편 변은 ⊗ → 힘이 **반대** → 회전.

학생이 "도선 주위의 자기장"과 "도선이 받는 힘"이 **한 도선의 이야기**임을 보게 하는 것이 전부다.

**방향은 기준 하나로 통일했다** (`js/emforce.js` 머리말):
`전류 +1 = ⊙ · 자석 +1 = N극 왼쪽 → 자기장 →` → `F = I × B` → (+z)×(+x) = **+y(위쪽)**.
그래서 **기본 구성이 선생님 표의 첫 칸(위쪽)과 저절로 일치**한다. 나머지 세 칸도 엔진이 낸 값이 표와 같다.

**힘의 크기는 N 으로 내놓지 않는다.** 학습지가 '비례한다'까지만 말하므로 **칸 = 전류(A) × 자석 수**로만 보인다.
전류는 `I = 6 V ÷ 저항`(저항 1~6 Ω)이라 **저항을 작게 하면 힘이 커진다**(형성평가와 같은 결론).

> ⚠ **범위 밖은 넣지 않았다** : 로런츠 힘 · 오른손/왼손 법칙의 이름(플레밍) · 힘의 정확한 식 · 자기장의 수치(T).
> ⚠ 전동기 회전 빠르기는 **전류 × 자석 수에 비례하는 값(0.35 rad/s 당 1칸)** 으로 정했다. 실제 전동기의 동역학이 아니라
> "센 힘 → 빨리 돈다"를 보이는 그림용 값이다. 정류자는 **선생님 학습지의 설명 그대로**(반 바퀴 뒤 전류가 끊기고 관성으로 돈다)로 그렸다.
> ⚠ 직선 도선의 나침반은 **지구 자기장을 무시**한다. 전류 0 이면 바늘이 모두 북쪽(위)을 가리키는 것으로만 그렸다.

### 자료 값 (학습지에 없어 이 앱이 정한 것)
전원 6 V · 저항 1~6 Ω(0.5 단위) → 전류 1~6 A · 자석 1~3개 · 코일 감은 수 6회 · 힘 1칸 = 1 A × 자석 1개.

### 🌐 배포 — GitHub Pages (2026-09-19 부터)

**학생은 GitHub 를 만지지 않는다. 주소 하나만 받는다.**

| 무엇 | 값 |
|---|---|
| 저장소 | https://github.com/science-creator/electromagnetic-force (**공개**) |
| 실험실 | https://science-creator.github.io/electromagnetic-force/ |
| 학습지 | https://science-creator.github.io/electromagnetic-force/worksheet.html |
| 선생님용 | https://science-creator.github.io/electromagnetic-force/teacher.html |
| 정답지 | https://science-creator.github.io/electromagnetic-force/worksheet.html?key |

> ⚠ 저장소가 공개라 **정답지 주소를 아는 사람은 누구나 정답을 본다.** 학생에게는 알리지 않는다(다른 앱도 같다).
> ⚠ 학습지는 선생님 검토 전이다. 학생에게 주소를 알리는 것은 검토 뒤에.
> ⚠ 첫 배포는 Pages 를 켠 뒤 **1~2분** 걸린다(처음 확인했을 때 잠깐 404 였다).

빌드가 없고 외부 의존성이 0개라 저장소의 파일이 그대로 웹 주소가 된다(`server.py` 는 로컬 확인용).
Pages 설정은 Source `Deploy from a branch` · `main` · `/ (root)`.
배포본에서도 실험실이 돌고(힘 방향 · 크기) 선생님 표 4칸이 그대로 나오는 것, 콘솔 오류 없음을 확인했다.

#### 고친 것을 반영하는 방법

```bash
git add .
git commit -m "무엇을 고쳤는지 한 줄"
git push
```

## ② 기능 목록

| 파일 | 설명 |
|---|---|
| `index.html` | 실험실. 장면 3개(코일 포함 4화면) + 미션 8개 |
| `worksheet.html` | 학습지 **20문항**(자동 채점 18) · `?key` 로 정답지 |
| `teacher.html` | 선생님용 제출물 정리 (파일 이름만 읽음) |
| `js/emforce.js` | 방향 · 크기 엔진 (`forceDir` · `wireNeedleAngle` · `coilPole` · `motorSpin` …) |
| `js/lab.js` | 실험실 화면 (`requestAnimationFrame` 은 🌀 ▶ 중에만 돈다) |
| `js/pdf.js` · `css/app.css` | **다른 앱과 같은 파일**(friction 과 바이트 단위로 동일) |
| `server.py` | 로컬 서버 (**9800번대**) |
| `로컬서버_실행.bat` | CP949 + CRLF (chcp 넣지 않음) |

## ③ 검증 (2026-09-19)

임시 정적 서버(PowerShell `HttpListener`)로 브라우저에서 돌려 확인했다.

### 방향과 값 — 그려진 것을 엔진과 대조

| 검증 내용 | 결과 |
|---|---|
| 뒤집기 규칙 | 하나만 바꾸면 반대 · 둘 다 바꾸면 그대로 (`flipRuleHolds`) |
| **선생님 표 4칸** | 위쪽 · 아래쪽 · 아래쪽 · 위쪽 — **엔진 값과 일치** |
| 직선 도선의 나침반 8개 × 2고리 | ⊙ → 접선 +90°(반시계) · ⊗ → −90°(시계) · 전류 0 → 북쪽 |
| 고리 굵기 | 안쪽 : 바깥 = **2 : 1** (거리에 반비례) · 전류에 비례 |
| 코일의 N극 | 앞쪽에서 아래로 → **오른쪽** · 위로 → 왼쪽 |
| **힘 화살표의 실제 그려진 길이** (캔버스 `lineTo` 를 가로챔) | 전류 4단계 × 자석 3종 × 방향 4조합 = **48조합 모두** (전류 × 자석 수 × 6 px) · 방향(위/아래)도 일치 |
| 자기장 선 수 | 2n + 1 줄 (자석 수에 따라) |
| 전동기 | AB · CD 가 회전축 **대칭 위치** · 전류 **반대** · 힘 **반대** · 반지름 일치 · 정류자 구간(cos θ ≤ 0)에서 화살표 없음 |
| 회전 방향 | ⊙·N왼쪽 → 반시계 / 전류 반대 → 시계 / **둘 다 반대 → 반시계** |
| 힘-전류 그래프의 점 | 자석 1·2·3개 모두 F = I × n 위 |
| 글자 넘침 | 375 px 폭에서 4화면 × 280개 상태 + 그래프 12개 상태 — **0건** |

### 미션 · 학습지

| 검증 내용 | 결과 |
|---|---|
| 미션 8개 | 성공 경로로 모두 `won`. **시작 상태는 8개 모두 목표 미달성** |
| 잘못된 상태로는 안 깨짐 | 미션 2(전류 0) · 4(스위치 꺼짐) · 6(하나만 바꿈) · 7(기본값) · 8(짧게) → `ready` 유지 |
| 재생 | 1 s 에 1.058 rad (기대 1.05) · 전류 반대로 하면 음의 방향 · 멈춤 유지 |
| 학습지 문항 | **20문항**(선택 16 · O/X 1 · 숫자 1 · 표 1 · 서술 1) · 자동 채점 **18** · 만점 18/18 |
| 정답 위치 분포 | 1번 6 · 2번 5 · 3번 5 (쏠림 없게 섞음) |
| 정답 | 그림과 정답이 **엔진에서 함께 나옴**(`mk()` · `circ()` · `upDown()` …). 힘 그림 (A) N-S/⊙ · (B) S-N/⊙ 를 좌표로 확인 |
| 정답지(`?key`) | 20문항 · 빈 정답 **0** |
| PDF | 5쪽 · 파일 이름 `전자기력학습지_2-4-12_…pdf` |
| 인쇄 | Edge PDF 인쇄로 **학습지 3쪽**(마지막 쪽은 4줄뿐) · 정답지 1쪽 |
| 콘솔 오류 | 없음 |
| `file://` 전제 | 모듈 · fetch · CDN · localStorage **0건** |

### 검증 중에 고친 것
1. **코일 그림의 나침반을 코일 아래에 뒀더니 방향이 반대인 자리**였다(되돌아오는 자기장 선). → 축 위 세 곳에만 둔다.
2. 힘 화살표의 글자가 S극 자석 위로 넘쳤다. → 화살표 끝 위/아래 가운데 정렬.
3. 화살표 눈금을 8 → 6 px/칸 (최대 18칸일 때 위 문구와 겹침).
4. 좁은 폭에서 **코일 끝이 자석에 닿았다.** → 코일 반지름을 자석 사이 간격에 맞춘다.
5. 정답이 2번 보기에 13/16 로 쏠렸다. → 보기 순서를 섞었다.
6. 학습지를 처음 37문항으로 썼다가 **20문항 이하**(선생님 요청)로 줄였다. 정답을 손으로 적지 않고 엔진에서 뽑도록 다시 짰다.

### 아직 못 한 것
**실제 프린터 출력**(쪽 수는 Edge PDF 인쇄로만 셈, 쪽 안의 모양은 눈으로 못 봄) · PDF 파일을 열어 눈으로 확인 ·
`file://` 더블클릭 · 태블릿 터치 · `server.py` 실행(파이썬 없음) ·
학습지 화면의 눈으로 본 확인(그림은 좌표로만 확인). 브라우저 창이 가려져 있으면 `requestAnimationFrame` 이 멈춰
재생은 타이머로 대신 돌려 확인했다.

### 다음 할 일
- [ ] 선생님이 학습지를 원본 hwp 와 견주어 검토 (특히 「연습하기」 그림 · 형성평가 회로)
- [ ] 종이로 한 번 뽑아 글자·그림 크기 확인
- [ ] 같은 단원의 남은 소단원 — **(1) 전기 (마찰전기 · 전류·전압·저항)** 는 `Electricity` 앱과의 관계를 확인
- [ ] 3학년 `electromagnetic-induction` 과 서로 링크 (전동기 ↔ 발전기)
- [x] GitHub 공개 + Pages 배포 (2026-09-19) — 여덟 주소가 열리고 배포본에서 동작하는 것까지 확인함

## 코드 메모
- **정답을 손으로 적지 말 것.** 학습지의 방향 정답은 `mk(list, correct)` 에 `circ()` · `upDown()` · `spin()` 결과를 넣어 만든다.
- 미션은 `MISSIONS` 배열에 항목만 넣으면 된다. `setup` 은 시작 상태(목표가 참이면 안 됨),
  `allow` 는 조작판에서 열어 줄 항목(`mode` `cur` `isign` `bsign` `sw` `R` `n`).
- 캔버스 검사용 구분 : 힘 화살표 줄기 lineWidth 6 · 나침반 몸통 arc r=13 · 전류 기호 arc r=15/14.
- `EfLab._test` (`set` `scene` `pick` `answer` `goals` `state` `advance` `geom` `draw`) · `EFSheet` 는 검사용 손잡이.
- hwp 는 이 PC 의 **한글 2022 자동화**(`HWPFrame.HwpObject` → `SaveAs(…, "TEXT")`)로 읽을 수 있다. 저장되는 글자는 CP949.
